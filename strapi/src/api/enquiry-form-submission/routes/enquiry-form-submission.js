'use strict';

/**
 * enquiry-form-submission router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::enquiry-form-submission.enquiry-form-submission');
